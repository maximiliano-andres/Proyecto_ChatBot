<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= titulo %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      background-color: #f5f7fa;
      color: #333;
      padding: 40px 0;
    }

    .section-title {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 3rem;
      text-align: center;
      color: #1e2a38;
    }

    .integrante {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 4rem;
      padding: 2rem;
      border-radius: 1rem;
      background: #fff;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.05);
    }

    .integrante:nth-child(even) {
      flex-direction: row-reverse;
      background: #f0f4f8;
    }

    .integrante img {
      width: 240px;
      height: 240px;
      object-fit: cover;
      border-radius: 50%;
      border: 5px solid #dee2e6;
    }

    .info {
      flex: 1;
      padding: 1rem 2rem;
    }

    .nombre {
      font-size: 1.8rem;
      font-weight: 700;
      color: #1e2a38;
    }

    .carrera {
      font-size: 1.2rem;
      color: #5c6b73;
    }

    .descripcion {
      font-size: 1rem;
      margin-top: 1rem;
      color: #6c757d;
    }

    .volver-inicio {
      text-align: center;
      margin-top: 3rem;
    }

    .btn-inicio {
      padding: 0.7rem 2rem;
      background-color: #0d6efd;
      color: white;
      border-radius: 5px;
      text-decoration: none;
      font-weight: 600;
      transition: background 0.3s;
    }

    .btn-inicio:hover {
      background-color: #0b5ed7;
    }

    @media (max-width: 768px) {
      .integrante {
        flex-direction: column !important;
        text-align: center;
      }

      .integrante img {
        margin-bottom: 1rem;
      }

      .info {
        padding: 0;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h2 class="section-title">Equipo de Trabajo - Universidad</h2>

    <% integrantes.forEach((persona, index) => { %>
      <div class="integrante">
        <img src="<%= persona.foto || './images/perfil/perfil_default.webp' %>" alt="Foto de <%= persona.nombre %>" />
        <div class="info">
          <h3 class="nombre"><%= persona.nombre %></h3>
          <p class="carrera"><strong>Carrera:</strong> <%= persona.carrera %></p>
          <p class="carrera"><strong>Descripcion:</strong> <%= persona.descripcion %></p>
          <% if (persona.descripcion) { %>
            <p class="descripcion"><%= persona.descripcion %></p>
          <% } %>
        </div>
      </div>
    <% }); %>

    <div class="volver-inicio">
      <a href="/" class="btn-inicio">Volver al Inicio</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

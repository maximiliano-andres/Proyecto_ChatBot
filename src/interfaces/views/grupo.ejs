<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= titulo %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet" />

  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      background: url('./images/back/universidad-mayor.jpg') no-repeat center center fixed;
      background-size: cover;
      color: #333;
    }

    .overlay {
      background-color: rgba(0, 0, 0, 0.658);
      min-height: 100vh;
      padding: 80px 20px;
    }

    h2.section-title {
      font-size: 2.75rem;
      font-weight: 700;
      color: hsl(0, 7%, 97%);
      margin-bottom: 4rem;
    }

    .card-integrante {
      background-color: #fff;
      border: none;
      border-radius: 1.5rem;
      box-shadow: 0 6px 30px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      padding-top: 90px;
      position: relative;
      min-height: 360px;
    }

    .card-integrante:hover {
      transform: translateY(-8px);
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
    }

    .avatar {
      width: 140px;
      height: 140px;
      object-fit: cover;
      border-radius: 50%;
      border: 5px solid #fff;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
      position: absolute;
      top: -70px;
      left: 50%;
      transform: translateX(-50%);
      background-color: #fff;
    }

    .card-body {
      padding-top: 1.5rem;
    }

    .nombre {
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .carrera {
      font-size: 1.1rem;
      color: #6c757d;
    }

    .volver-inicio {
      margin-top: 60px;
    }

    .btn-inicio {
      padding: 12px 30px;
      font-size: 1.2rem;
      font-weight: 600;
      border-radius: 2rem;
      background-color: #0d6efd;
      color: white;
      transition: background-color 0.3s ease, transform 0.2s ease;
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
    }

    .btn-inicio:hover {
      background-color: #0b5ed7;
      transform: scale(1.05);
    }

    @media (max-width: 767px) {
      h2.section-title {
        font-size: 2rem;
      }

      .avatar {
        width: 110px;
        height: 110px;
        top: -55px;
      }

      .nombre {
        font-size: 1.25rem;
      }

      .btn-inicio {
        font-size: 1rem;
        padding: 10px 24px;
      }
    }
  </style>
</head>
<body>
  <div class="overlay text-center">
    <h2 class="section-title">Equipo de Trabajo - Universidad</h2><br><br>
    <div class="container">
      <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-5">
        <% integrantes.forEach((persona) => { %>
          <div class="col">
            <div class="card card-integrante">
              <img src="<%= persona.foto || './images/perfil/perfil_default.webp' %>" class="avatar" alt="Foto de <%= persona.nombre %>">
              <div class="card-body">
                <h5 class="nombre"><%= persona.nombre %></h5>
                <p class="carrera"><%= persona.rol %></p>
              </div>
            </div>
          </div>
        <% }); %>
      </div>

      <div class="volver-inicio mt-5">
        <a href="/" class="btn btn-inicio">Volver al Inicio</a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

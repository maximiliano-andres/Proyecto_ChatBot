<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visualización de PDF</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, sans-serif; background-color: #e9ecef; color: #333; }
        .container { max-width: 900px; margin: 50px auto; padding: 30px; background: #ffffff; box-shadow: 0px 0px 20px rgba(0,0,0,0.1); border-radius: 15px; }
        .btn { padding: 15px 30px; margin: 15px 0; text-decoration: none; color: #ffffff; border-radius: 10px; font-size: 16px; font-weight: bold; display: inline-block; transition: background-color 0.3s ease; }
        .btn-primary { background-color: #4CAF50; }
        .btn-secondary { background-color: #2196F3; }
        .btn:hover { opacity: 0.9; }
        iframe { width: 100%; height: 700px; border-radius: 10px; border: none; margin-bottom: 20px; }
        .error { color: red; font-size: 18px; margin-top: 10px; font-weight: bold; }
        h1 { font-size: 32px; margin-bottom: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>📄 Vista de Documento PDF</h1>
        <% if (vista_pdf) { %>
            <iframe src="../../PDF/contrato_123.456.789.pdf" allowfullscreen type="application/pdf"></iframe>
        <% } else { %>
            <p class="error">❌ El archivo PDF no está disponible.</p>
        <% } %>
        <div>
            <a href="/" class="btn btn-secondary">🏠 Volver al Home</a>
            <a href="/firmar-contrato" class="btn btn-primary">🖋️ Firmar Contrato</a>
        </div>
    </div>
</body>
</html>
